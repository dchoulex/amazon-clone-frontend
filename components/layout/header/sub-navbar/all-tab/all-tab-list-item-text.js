import { styled } from "@mui/material/styles";
import ListItemText from "@mui/material/ListItemText";

const AllTabListItemText = styled(ListItemText)(() => ({
    color: "#484848",
    fontWeight: "lighter"
}));

export default AllTabListItemText;